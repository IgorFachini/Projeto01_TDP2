@model Supermercado.Models.ConteudoPorGenero

<h2>Gênero: @Model.Genero</h2>

@foreach (var item in Model.Livros)
{
    <div class="col-sm-12 col-md-6" style="margin-bottom: 3rem">
        <div class="thumbnail">
            <img src="~/Content/img/flat-book.png" alt="Imagem exemplo de um livro" />
            <div class="caption">
                <h3 title="@item.Titulo">@item.Titulo.Substring(0, item.Titulo.Length > 32 ? 32 : item.Titulo.Length)</h3>
                <p>Autor: @item.Autor</p>
                <p>Ano da edição: @item.AnoEdicao</p>
                <p>Tipo: @item.TipoId</p>
                <p>@($"R$ {item.Valor}")</p>
            </div>
        </div>
    </div>
}

@foreach (var item in Model.Games)
{
    <div class="col-sm-12 col-md-6" style="margin-bottom: 3rem">
        <div class="thumbnail">
            <img src="~/Content/img/flat-book.png" alt="Imagem exemplo de um livro" />
            <div class="caption">
                <h3 title="@item.Titulo">@item.Titulo.Substring(0, item.Titulo.Length > 32 ? 32 : item.Titulo.Length)</h3>
                <p>Plataforma: @item.Plataforma</p>
                <p>Ano do Lançamento: @item.AnoLancamento</p>
                <p>Genero: @item.TipoId</p>
                <p>@($"R$ {item.Valor}")</p>
            </div>
        </div>
    </div>
}

@foreach (var item in Model.Filmes)
{
    <div class="col-sm-12 col-md-6" style="margin-bottom: 3rem">
        <div class="thumbnail">
            <img src="~/Content/img/flat-book.png" alt="Imagem exemplo de um livro" />
            <div class="caption">
                <h3 title="@item.Titulo">@item.Titulo.Substring(0, item.Titulo.Length > 32 ? 32 : item.Titulo.Length)</h3>
                <p>Plataforma: @item.Diretor</p>
                <p>Ano do Lançamento: @item.AnoLancamento</p>
                <p>Tipo: @item.TipoId</p>
                <p>@($"R$ {item.Valor}")</p>
            </div>
        </div>
    </div>
}






